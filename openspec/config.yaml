schema: spec-driven

context: |
  Tech stack: Python (yt-dlp, pytest)
  Architecture: CLI Tool
  Testing: pytest
  Style: default

strict_tdd: true

testing:
  runner: pytest
  layers:
    unit: true
    integration: false
    e2e: false
  coverage: false
  quality_tools:
    linter: false
    type_checker: false
    formatter: false
